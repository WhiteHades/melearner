# Local files load through Tauri assets

Local media and documents load through Tauri asset URLs instead of a localhost media server. This keeps runtime architecture offline and direct while still letting the WebView render files selected by the user.
